const {
    EmbedBuilder
} = require('discord.js');

module.exports.run = async (client, interaction, args) => {
    try {
        
    } catch (error) {
        console.error(error)
    }
}