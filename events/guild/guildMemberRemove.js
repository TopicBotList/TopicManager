const {
    EmbedBuilder,
    ActivityType
} = require('discord.js');

module.exports.run = async (client, message, args) => {
    try {
        
    } catch (error) {
        console.error(error)
    }
}