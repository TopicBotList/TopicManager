const {
    EmbedBuilder,
    Colors
} = require('discord.js');

module.exports = {
    name: "eval",
    description: "Evaluate some Code.",
    cooldown: "3",
    disabled: false,
    admins: true,
    run: async (client, interaction, args) => {
        
    }
}