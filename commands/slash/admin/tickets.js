const {
    EmbedBuilder,
    Colors
} = require('discord.js');

module.exports = {
    name: "tickets",
    description: "Sends the ticket embed to the tickets channel.",
    cooldown: "3",
    disabled: false,
    admins: true,
    run: async (client, interaction, args) => {
        
    }
}